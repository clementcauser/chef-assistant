{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/chef-assistant",
  "projectType": "application",
  "targets": {
    "build": {
      "executor": "@nrwl/next:build",
      "outputs": ["{options.outputPath}"],
      "defaultConfiguration": "production",
      "options": {
        "root": "apps/chef-assistant",
        "outputPath": "dist/apps/chef-assistant"
      },
      "configurations": {
        "development": {
          "outputPath": "apps/chef-assistant"
        },
        "production": {}
      }
    },
    "serve": {
      "executor": "@nrwl/next:server",
      "defaultConfiguration": "development",
      "options": {
        "buildTarget": "chef-assistant:build",
        "dev": true
      },
      "configurations": {
        "development": {
          "buildTarget": "chef-assistant:build:development",
          "dev": true
        },
        "production": {
          "buildTarget": "chef-assistant:build:production",
          "dev": false
        }
      }
    },
    "export": {
      "executor": "@nrwl/next:export",
      "options": {
        "buildTarget": "chef-assistant:build:production"
      }
    },
    "test": {
      "executor": "@nrwl/jest:jest",
      "outputs": ["coverage/apps/chef-assistant"],
      "options": {
        "jestConfig": "apps/chef-assistant/jest.config.ts",
        "passWithNoTests": true
      }
    },
    "lint": {
      "executor": "@nrwl/linter:eslint",
      "outputs": ["{options.outputFile}"],
      "options": {
        "lintFilePatterns": ["apps/chef-assistant/**/*.{ts,tsx,js,jsx}"]
      }
    }
  },
  "tags": []
}
